import React from "react";
import ReactDOM from "react-dom/client";
import TNovaWebsite from "./TNovaWebsite.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TNovaWebsite />
  </React.StrictMode>
);
